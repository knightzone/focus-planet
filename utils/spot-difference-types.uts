export type DifferenceCandidate = { id: string, x: number, y: number, w: number, h: number, left: string, top: string, width: string, height: string, difficulty: number, zone: number }
export type DifferenceScene = { id: string, tier: string, imageA: string, imageB: string, regions: DifferenceCandidate[] }
