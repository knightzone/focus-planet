export type AgeBand = {
  id: string,
  label: string,
  min: number,
  max: number,
  sessionMinutes: number
}

export type TrainingGame = {
  id: string,
  dimensionId: string,
  name: string,
  summary: string,
  status: string
}

export type FocusDimension = {
  id: string,
  name: string,
  shortName: string,
  color: string,
  description: string,
  thumbnail: string
}

export const AGE_BANDS: AgeBand[] = [
  { id: 'starter', label: '启蒙模式', min: 3, max: 6, sessionMinutes: 5 },
  { id: 'growth', label: '成长模式', min: 7, max: 9, sessionMinutes: 10 },
  { id: 'advanced', label: '进阶模式', min: 10, max: 12, sessionMinutes: 15 },
  { id: 'teen', label: '青少年模式', min: 13, max: 16, sessionMinutes: 25 }
]

export const DIMENSIONS: FocusDimension[] = [
  { id: 'selective', name: '观察发现', shortName: '观察', color: '#FFC857', description: '找目标、辨细节、排除干扰', thumbnail: '/static/images/runtime/dimensions/selective.webp' },
  { id: 'focused', name: '记忆辨识', shortName: '记忆', color: '#5B8DEF', description: '记住信息，辨认声音与图像', thumbnail: '/static/images/runtime/dimensions/focused.webp' },
  { id: 'sustained', name: '反应控制', shortName: '反应', color: '#55C9A5', description: '抓准时机，控制动作与节奏', thumbnail: '/static/images/runtime/dimensions/sustained.webp' },
  { id: 'alternating', name: '逻辑空间', shortName: '逻辑', color: '#9B7EDE', description: '找规律、做规划、理解形状与整体', thumbnail: '/static/images/runtime/dimensions/alternating.webp' },
  { id: 'divided', name: '切换兼顾', shortName: '兼顾', color: '#F17C8D', description: '切换判断规则，兼顾不同信息', thumbnail: '/static/images/runtime/dimensions/divided.webp' }
]

export const GAMES: TrainingGame[] = [
  { id: 'star-catcher', dimensionId: 'sustained', name: '星星捕手', summary: '目标出现后快速点击', status: 'ready' },
  { id: 'sound-location', dimensionId: 'focused', name: '声音从哪来', summary: '听声音，找到对应的图片', status: 'ready' },
  { id: 'shape-match', dimensionId: 'alternating', name: '影子配对', summary: '把卡通图案送回对应影子', status: 'ready' },
  { id: 'light-tracking', dimensionId: 'focused', name: '萤火虫追踪', summary: '记住路径并按顺序复现萤火虫的移动', status: 'ready' },
  { id: 'number-tap', dimensionId: 'selective', name: '数字连点', summary: '按顺序完成三组 9 至 25 个数字', status: 'ready' },
  { id: 'penalty-kick', dimensionId: 'divided', name: '点球小将', summary: '观察门将并选择射门方向', status: 'ready' },

  { id: 'sky-watcher', dimensionId: 'sustained', name: '星空守望员', summary: '长时间观察指定目标', status: 'ready' },
  { id: 'car-patrol', dimensionId: 'sustained', name: '小车巡逻', summary: '保持路线并避开障碍', status: 'ready' },
  { id: 'spot-change', dimensionId: 'focused', name: '找出变化', summary: '发现画面中的偶发变化', status: 'ready' },
  { id: 'breathing-planet', dimensionId: 'sustained', name: '呼吸小星球', summary: '跟随节奏完成连续操作', status: 'ready' },
  { id: 'audio-sequence', dimensionId: 'focused', name: '听音节奏', summary: '记住目标声音，试听并找出相同的一项', status: 'ready' },
  { id: 'bird-cloud', dimensionId: 'sustained', name: '小鸟穿云', summary: '持续调整高度穿过云门', status: 'ready' },

  { id: 'forest-search', dimensionId: 'selective', name: '森林寻宝', summary: '在复杂场景中寻找物品', status: 'ready' },
  { id: 'color-command', dimensionId: 'selective', name: '颜色指令', summary: '忽略干扰并选择目标颜色', status: 'ready' },
  { id: 'sound-filter', dimensionId: 'divided', name: '声音过滤器', summary: '听清类型，点出同一类的气泡', status: 'ready' },
  { id: 'symbol-detective', dimensionId: 'selective', name: '图案侦探', summary: '找齐不同的图案', status: 'ready' },
  { id: 'spot-difference', dimensionId: 'selective', name: '找茬乐园', summary: '比较两幅场景并找出不同', status: 'ready' },
  { id: 'puzzle', dimensionId: 'alternating', name: '拼图工坊', summary: '看参考图把拼块放回原位', status: 'ready' },

  { id: 'magic-rule', dimensionId: 'divided', name: '魔法规则', summary: '在颜色与形状规则间切换', status: 'ready' },
  { id: 'sorting-station', dimensionId: 'alternating', name: '分类车站', summary: '按照变化的规则分类', status: 'ready' },
  { id: 'number-track', dimensionId: 'divided', name: '奇偶轨道', summary: '看单双数字，切换字母大小写规则', status: 'ready' },
  { id: 'red-green', dimensionId: 'sustained', name: '红绿指令', summary: '红灯停进安全区，绿灯及时出发', status: 'ready' },
  { id: 'water-sort', dimensionId: 'alternating', name: '倒水小实验', summary: '规划步骤并把颜色分开', status: 'ready' },
  { id: 'pattern-find', dimensionId: 'alternating', name: '规律小侦探', summary: '观察图案顺序并补全规律', status: 'ready' },
  { id: 'order-guess', dimensionId: 'alternating', name: '顺序猜猜', summary: '根据正确个数，推理图片的隐藏顺序', status: 'ready' },

  { id: 'little-driver', dimensionId: 'divided', name: '一心二用小司机', summary: '一边躲路锥，一边看住车窗外', status: 'ready' },
  { id: 'listen-find', dimensionId: 'selective', name: '听声捉图形', summary: '听指令，点出对应位置的图形', status: 'ready' },
  { id: 'memory-search', dimensionId: 'focused', name: '记忆与搜索', summary: '记住目标并完成寻找', status: 'ready' },
  { id: 'memory-link', dimensionId: 'focused', name: '记忆连线', summary: '记住图形的数字，打乱后连线配对', status: 'ready' },
  { id: 'dual-track', dimensionId: 'divided', name: '上天入海', summary: '上下躲开星球和鱼，两队都不能过中线', status: 'ready' }
]

export function getAgeBand(age: number): AgeBand {
  for (let i = 0; i < AGE_BANDS.length; i++) {
    if (age >= AGE_BANDS[i].min && age <= AGE_BANDS[i].max) return AGE_BANDS[i]
  }
  return AGE_BANDS[0]
}

export function gamesForDimension(dimensionId: string): TrainingGame[] {
  return GAMES.filter((game: TrainingGame): boolean => game.dimensionId == dimensionId)
}

export function getGameName(gameId: string): string {
  const game = GAMES.find((item: TrainingGame): boolean => item.id == gameId)
  return game == null ? '专注训练' : game.name
}

// 保留协议ID，分类语义以本目录为准；页面/结算不再各自维护归属。
export function getGameDimensionId(gameId: string): string {
  const game = GAMES.find((item: TrainingGame): boolean => item.id == gameId)
  return game == null ? '' : game.dimensionId
}

export function getGameThumbnail(gameId: string): string {
  return '/static/images/runtime/level-thumbnails/' + gameId + '.webp'
}

export function getGameRoute(gameId: string): string {
  // memory-search 已有独立页面，走下方默认拼接（/pages/game/memory-search）；
  // grid-challenge 只保留 spot-change / symbol-detective 两种模式。
  if (gameId == 'spot-change' || gameId == 'symbol-detective') {
    return '/pages/game/grid-challenge?gameId=' + gameId
  }
  // 一心二用小司机已有独立页面（2026-09-18 重做：躲路锥 + 底部安全滚动条），走下方默认拼接。
  if (gameId == 'light-tracking') {
    return '/pages/game/motion-challenge?gameId=' + gameId
  }
  return '/pages/game/' + gameId
}
