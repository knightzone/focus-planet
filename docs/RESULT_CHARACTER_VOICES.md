# 双角色结算语音

2026-09-13。替代旧四句通用结算语音。

最新UI精简：结束页移除单独的角色名字、停止语音按钮及下次难度卡片；保留角色图片、台词（包括自然自称）和一次自动播放，后台/离开仍自动停止。难度更新继续执行并持久化，只是不再展示升级信息。

- 暖暖：晓晓 zh-CN-XiaoxiaoNeural，温柔陪伴。
- 跃跃：云夏 zh-CN-YunxiaNeural，偏童声/少年感，活泼探索。此次先按该音色接入，待用户真机听感确认。
- 两位均采用-8%语速、自然音高、-21LUFS目标响度、24kHz单声道64kbps离线MP3。
- 三档：score>=90直接夸奖；60<=score<90正常反馈；score<60温柔鼓励。每档每位角色两句，共12句。完整台词与角色/文件映射统一维护于 `utils/result-dialogue.uts`，部分使用自称，不在运行时拼接音频。
- 同分数段随机选一句，相邻结算避免重复同一条；先得到最终goal-v2分数，再一次选定角色、图片、台词及声音，首次挂载后仅播放一次，不在重绘/回前台时重选。
- 结算中心使用蓝兔/黄猫各三张真实透明表情图，按同一条台词的role与band匹配：夸奖开心鼓掌，正常微笑抱星星手账，鼓励温柔捧爱心，不使用悲伤或失望表情。显示角色名。>=120分时五颗主角星环绕角色，原星级和报告第六星规则不变。90分夸奖不等于100分过关，也不等于120分特殊动画。
- 六张384×384透明WebP合计110,452字节（约108KiB），位于 `static/images/runtime/result-characters/`。移除旧圆角裁切，解决不透明矩形遮挡环绕星的问题。原PNG与生成提示词见 [素材说明](../content/brand/result-expressions/README.md)。`tools/test-result-expressions.cjs` 检查三档边界、六张路径、解码、alpha及透明边角。
- 原三种正确/错误/错过音保留。旧finish-1至finish-4移到 `outputs/audio-review/character-dialogues-v1/legacy-finish-*.mp3`，可恢复、不再进入运行包。
- 当前15个运行音频合计491,268字节（约480KiB）。生成入口 `tools/build-character-dialogues.cjs --install`；先校验全部12句再安装。只向在线服务发送公共角色台词，无用户资料。正式发行仍需确认声音服务许可。

验证：专项覆盖60/90边界、两角色及12条映射、所有素材可解码、单次播放/后台停止/卸载清理、星级动画及27游戏存储上传分数一致性。浏览器预览检查角色环绕和整体布局；双端编译与Xcode同步状态记入路线图。
