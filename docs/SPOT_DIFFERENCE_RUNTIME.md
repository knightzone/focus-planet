# 找茬乐园：故事素材运行接入

## 最新：16组补充60处（2026-09-20）

041、057、090、095、098、144原有3候选，各加5处；010、065、076、085、112、115、116、121、126、149各加3处。150组现有1049候选，全部能同轮选满5处，仍随机抽取2—5处，不固定前几项。候选数分布：5处11组、6处50组、7处42组、8处29组、9处12组、10处6组。

新增包括整件物品移除、新增玩具/蝴蝶、旗形/网形/物品形状替换及清晰图案变化。16组已逐图检查q30运行效果。保留全部旧候选坐标、删除决定及原始章节A/B；新来源与60处坐标在 `runtime-supplements.json`，原始生成及提示词归档在 `content/ai-assets/archive/spot-difference-story-v2/supplement-20260920/`。合成器旧区域仍读取原B，新区域读取补充图，绝不整张覆盖；新增与旧来源区域不交叉，同源区域的少量矩形交集继续互斥。

压缩仍768×432、WebP q30，300张共9,477,352字节，约9.04MiB，仅比补充前增加1,908字节。审核入口 `runtime-review/below-five-regions.html` 现在固定展示这16组补充结果，按钮标出“新增”，默认仅展示兼容的新候选。重新生成用 `node tools/review-independent-story-regions.cjs --supplemented`；`--below-five` 则重新查询当前不足5处的组（当前0组）。

验证完成：150组1049候选压缩像素及框外一致性、12000次互斥配方、500次游戏流程、轮换及扩大点击区、16组审核页、600静态引用均通过。15:54完成HBuilderX 5.25编译/导出及Xcode同步，新鲜度与输入检查通过；宿主300张WebP逐字节一致，1049个编译候选坐标全部一致。未做本轮真机Run/Archive。以下为历史记录。

## 当前调整：独立差异，不要求物理联动（2026-09-20）

本轮处理58组、74个原候选，题库现为150组、989候选（此前865）。122的围巾、敞口箱、带盖箱分别选择；敞口箱从围巾下方开始裁切，不再带走整条围巾。纸船与明显倒影分别选择，灯笼与吊穗、吊牌图案与吊绳、车身与轮心、围巾与杯瓶等均不再因为物理关联强制捆绑。

逐候选合成目检后，移除仍会包含子物件的大父框：071镜框、079屏风外框、084工具板底座、130沙盘底座、139水盘、137绘本下坐垫；保留其中细分物件。033、036、039的4个弱倒影不加入候选，034/038的明显倒影独立保留。134中间彩旗本身未变，不以邻居变化凑数。087/100车身、109篮中蝴蝶结、143篮体、076灯笼进一步避开邻近物品。以上是候选调整，不删除整组或源图。完整本轮记录：`expanded-region-splits.json`；收紧理由：`expanded-region-refinements.json`；累计记录：`independent-region-splits.json`。

继续使用矩形裁切及几何互斥，不是轮廓蒙版：相邻物品可能有部分进入同一矩形，但重叠候选绝不能同轮出现。未经选中的独立矩形不会主动叠加。q30及768×432不变，共9,475,444字节（约9.04MiB）。全150组/989候选压缩像素、框外一致性、源标注一致性、11,540次随机互斥与扩大点击区回归通过。

14:40完成iOS 5.25编译、导出、Xcode同步及新鲜度校验；逐字节核对300张WebP一致，直接检查宿主编译代码中全部989个候选坐标也与最终清单一致。未进行本轮真机Run/Archive。按工程资源流水线同步，不手改导出产物。

候选数分组：3处6组、4处6组、5处13组、6处52组、7处36组、8处21组、9处10组、10处6组。考虑互斥后，有134组可同轮选满5处；其余16组只参与能凑足的较少差异档位，不少出题兜底。以下为历史记录。

同步状态（2026-09-20 13:40）：已通过 `prepare-xcode.sh` 完成 HBuilderX 5.25 蒸汽模式编译、iOS导出、Xcode宿主同步及最终校验。资源新鲜度通过，AppID为 `__UNI__B5780B0`；300张故事WebP与主项目逐字节一致。最新互斥出题/点击区修复已随本次编译导出。以下历史“待同步”状态由此记录取代；尚未执行本次真机Run或Archive。

最新追加（2026-09-20）：用户确认矩形允许少量包含相邻图案，但有交集的候选不能同轮出现。已实现几何互斥＋回溯抽样，选图/轮换按能否凑足互不冲突的2—5处过滤，不再只看候选总数；不以少出题兜底。审核页共用游戏规则，手选会取消冲突项。扩大点击区按原始坐标分隔，修复像素换算浮点误差导致贴边目标仍重叠的问题。

累计19组的20个合并区拆成52个候选，当前150组、865候选。新增拆分073、110、120、121、124、141，087车身/轮心允许边界重叠并互斥。合成器使用最大透明度并集，重叠处不因合成顺序擦掉另一矩形内部。逐项审核见 `runtime-review/independent-regions.html`，实际随机配方见 `runtime-review/recipes.html`。116「左边花朵」、129「手中花朵」删除决定保留；源图未修改，iOS尚待重新成功导出同步。

## 2026-09-20：用户复核可辨识度

按用户意见删除 010 左轮、011 花盆纹、019 卷垫、110 喷泉中心；保留 018 熊围巾。修正 050 篮子图案、106 前景花朵、149 花灯、150 小鼓图画的区域。116 花朵源图未找到明确变化，恢复原范围待确认；129 未指定处理，保持不变。现为 150 组、835 候选。首批 001—034“不改动”的旧记录自此次明确指定删除起有例外。详见 `SPOT_DIFFERENCE_VISIBILITY_AUDIT.md`。

## 2026-09-20：恢复首批物品级替换策略

当前扩展至150组。035—150原先自动按颜色差异密度挑小方框，容易只替换物品中央；后来扩大合成范围1.9倍但没有同步游戏裁切边界，且像素检查也放宽了范围，不能证明游戏里的物品完整。本次取消上述两种策略。

- 001—034的人工区域保持不变；035—150逐图对照A/B重标物品边界，记录在 `reviewed-object-regions.json`，通过导入脚本更新 `runtime-regions.json`。
- 所有图片统一沿用001—034的矩形区域复制、3像素边缘柔化。合成、游戏裁切、点击目标使用同一套坐标，不额外扩大、不按颜色掩膜挖取。
- 范围覆盖A/B两种物品外形；允许经过人工审核的相邻矩形少量重叠，任意正面积交集自动互斥（同zone仍互斥）。不得为凑数切碎物品；严重粘连的组合仍需单独审核，不自动扩大所有矩形。
- 每图至少能选出2个互不冲突的候选。出题按本级需要的2—5处实际可选数量过滤图片，不改变原来等级配比。轮换仅重置本轮可用图片，不丢失其他图片的已看记录。
- 全部图仍为768×432、固定WebP质量30；源图不改动，不重新生成艺术素材。
- `runtime-review/index.html`是全部候选B的审核页；`runtime-review/recipes.html`才是每次选择2—5处、同一侧局部替换的实际裁切预览。章节 `review-ab.html` 是源图对照，不代表游戏合成结果。
- 检查取消1.9倍放宽，严格按真实候选区域检查框外变化，同时校验生成索引坐标与合成清单完全一致。像素检查不能替代实机视觉验收。

下面保留首批接入记录；当前进度与本次验证结果见制作目录的 `PRODUCTION_PROGRESS.md`。

2026-09-14。按用户要求暂停生成，自动任务2-15已删除。

## 已接入内容

SD2-001—034，34组68张；低/中/高档分别15/9/10组（2026-09-18 起改由画面复杂度实测定档：综合分 <50 低档、50—70 中档、>70 高档，不再按编号循环；口径与明细见 [复杂度与难度定档](SPOT_DIFFERENCE_COMPLEXITY_DIFFICULTY.md)）。运行资源768×432 WebP，统一质量30，总计2,314,392字节（约2.21 MiB）；原始PNG、被替换的姿势版本、提示词等仍在制作目录。旧样本校准及固定质量策略见 [压缩说明](SPOT_DIFFERENCE_COMPRESSION.md)。

当前239个可选局部区域，每图至少5个。原B存在背景重绘，因此不能直接整张展示。打包脚本从A底图只叠加标注区域并柔化边缘，游戏再随机选择2—5个局部显示于同一侧。没有选中的差异不会出现在游戏中。邻近同一道具组（例如木槌与垫布）可作为一个整体找茬目标。

不是宣称所有原B的10个差异均已验收；未选中的候选留在源素材，当前不参与出题。

## 规则

|等级|每局素材配比|每组不同数|
|---|---|---|
|1|低、低、中|2|
|2|低、中、高|2|
|3|低、中、高|3|
|4—5|低、中、中、高|3|
|6|低、中、高、高|4|
|7—8|低、中、中、高、高|4|
|9—10|低、中、中、高、高|5|

各档独立记录已看场景ID，不等答完才消耗。点击换图也会跳过已显示的场景；仅预加载、加载失败不消耗。该档全部看过后开新一轮，避免前后两张相同。资源补充时，新ID自然优先；不修改已有场景ID。

每轮按现有目标时间与误点/提示计算0—200分，整局取轮次平均；不把轮次总分相加。提示、正确/错误反馈、暂停继续/重新训练/退出、双图扩大点击区保留。加载阶段不能计分；两张资源就绪后计时，超时12秒可重试，旧回调不影响新场景。

## 后续维护

1. 在章节manifest保存独立A/B源图及来源记录。
2. 对照实际图片标注完整物品候选（百分比坐标，可少量重叠但同轮互斥，每图至少可同时选2个）；035—150维护reviewed-object-regions.json，执行prepare-story-runtime-regions.py导入，不能再自动按色差密度选框。
3. 运行tools/build-story-runtime.cjs，产出运行WebP、utils/spot-difference-story.uts及runtime-review索引；只转换已有图，不调用生成服务。
4. 执行tools/review-story-runtime-recipes.py，检查runtime-review/recipes.html实际2—5处裁切，不仅看全部候选B；运行test-story-runtime.cjs和三个test-spot-difference-*.cjs测试。
5. iOS执行ios/scripts/prepare-xcode.sh再用Xcode运行；Android通过HBuilderX重新编译运行。不能仅把图片复制到旧iOS包就认为代码同步。

## 验证范围

检查全部34对尺寸、每个候选压缩后真实变化、候选外背景一致性；模拟500组配方、10档完整结算、循环去重、扩大点击区、暂停、加载失败/超时/重试/重复/过期回调。iOS 5.25蒸汽模式资源编译及同步已通过；最终同步结果见当次交付。尚不等同于Android/iOS实机视觉与触控验收。
