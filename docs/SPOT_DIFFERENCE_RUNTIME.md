# 找茬乐园：故事素材首批运行接入

2026-09-14。按用户要求暂停生成，自动任务2-15已删除。

## 已接入内容

SD2-001—034，34组68张；低/中/高档分别12/11/11组。运行资源768×432 WebP，统一质量30，总计2,314,392字节（约2.21 MiB）；原始PNG、被替换的姿势版本、提示词等仍在制作目录。旧样本校准及固定质量策略见 [压缩说明](SPOT_DIFFERENCE_COMPRESSION.md)。

当前239个可选局部区域，每图至少5个。原B存在背景重绘，因此不能直接整张展示。打包脚本从A底图只叠加标注区域并柔化边缘，游戏再随机选择2—5个局部显示于同一侧。没有选中的差异不会出现在游戏中。邻近同一道具组（例如木槌与垫布）可作为一个整体找茬目标。

不是宣称所有原B的10个差异均已验收；未选中的候选留在源素材，当前不参与出题。

## 规则

|等级|每局素材配比|每组不同数|
|---|---|---|
|1|低、低、中|2|
|2|低、中、高|2|
|3|低、中、高|3|
|4—5|低、中、中、高|3|
|6|低、中、高、高|4|
|7—8|低、中、中、高、高|4|
|9—10|低、中、中、高、高|5|

各档独立记录已看场景ID，不等答完才消耗。点击换图也会跳过已显示的场景；仅预加载、加载失败不消耗。该档全部看过后开新一轮，避免前后两张相同。资源补充时，新ID自然优先；不修改已有场景ID。

每轮按现有目标时间与误点/提示计算0—200分，整局取轮次平均；不把轮次总分相加。提示、正确/错误反馈、暂停继续/重新训练/退出、双图扩大点击区保留。加载阶段不能计分；两张资源就绪后计时，超时12秒可重试，旧回调不影响新场景。

## 后续维护

1. 在章节manifest保存独立A/B源图及来源记录。
2. 对照实际图片，在content/brand/spot-difference-story-v2/runtime-regions.json标注候选（百分比坐标，不能重叠，每图至少5个）。
3. 运行tools/build-story-runtime.cjs，产出运行WebP、utils/spot-difference-story.uts及runtime-review索引；只转换已有图，不调用生成服务。
4. 检查runtime-review/index.html实际局部合成；运行test-story-runtime.cjs和三个test-spot-difference-*.cjs测试。
5. iOS执行ios/scripts/prepare-xcode.sh再用Xcode运行；Android通过HBuilderX重新编译运行。不能仅把图片复制到旧iOS包就认为代码同步。

## 验证范围

检查全部34对尺寸、每个候选压缩后真实变化、候选外背景一致性；模拟500组配方、10档完整结算、循环去重、扩大点击区、暂停、加载失败/超时/重试/重复/过期回调。iOS 5.25蒸汽模式资源编译及同步已通过；最终同步结果见当次交付。尚不等同于Android/iOS实机视觉与触控验收。
